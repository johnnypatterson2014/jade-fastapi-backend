# jade-fastapi-backend

create virtual env:
conda create -n jade python=3.12.0

activate it:
conda activate jade

install packages:
pip install -r requirements.txt

run server:
uvicorn main:app --reload

url:
http://127.0.0.1:8000

